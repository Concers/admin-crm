{
  "name": "crm-proje",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend install && npm --prefix backend run build && npm --prefix frontend run build",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "backend:dev": "npm --prefix backend run dev",
    "backend:build": "npm --prefix backend run build",
    "db:migrate": "npm --prefix backend exec prisma migrate deploy",
    "db:import": "npm --prefix backend run db:import",
    "db:seed-users": "npm --prefix backend run db:seed-users"
  }
}
